<div class="steamy-strudel">
	<h2>
		my first form in ruby on rails using simple_form!!!!!!!
	
		<%= simple_form_for @pic do |f| %>
			<%= f.input :hour %>
			<%= f.input :emotion %>
			<%= f.input :learned %>

			<%= f.submit 'Save' %>
		<% end %>

	</h2>
</div>